server:
  port: 9999
  servlet:
    context-path: /config

spring:
  profiles:
    active: git
  application.name: config-server
  cloud.config.server:
    git:
      uri: file://home/roman/github_my/config-service
      order: 1

